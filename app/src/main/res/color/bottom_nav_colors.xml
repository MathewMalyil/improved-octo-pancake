<?xml version="1.0" encoding="utf-8"?>
<!-- res/color/bottom_nav_colors.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active (checked) item uses brand/primary -->
    <item android:state_checked="true" android:color="?attr/colorPrimary"/>
    <!-- Inactive items follow theme (auto light/dark) -->
    <item android:color="?attr/colorOnSurface"/>

        <item android:color="@color/md3_primary" android:state_checked="true"/>
        <item android:color="@android:color/darker_gray"/>



</selector>